import { Link } from "react-router-dom";
import { ExternalLink } from "lucide-react";
import PageShell from "../components/PageShell";
import Breadcrumbs from "../components/Breadcrumbs";
const endpoints=["GET /api/current-aqi — current station conditions","GET /api/forecast — station forecast and 24-hour trend","GET /api/source-attribution — source contribution breakdown","GET /api/advisory — ward health guidance","GET /api/recommendations — mitigation and enforcement priorities","GET /api/reports — CSV or PDF export"];
export default function Documentation(){return <PageShell><main className="page-wrap"><Breadcrumbs items={[{label:"Help & documentation"}]}/><span className="eyebrow">Get oriented</span><h1 className="page-title">Help & documentation</h1><p className="page-lead">UrbanAQI is a local-first demonstration of an air-quality decision support workflow.</p><div className="doc-grid"><section><h2>Using the platform</h2><ol><li>Open the command center to view the live local data feed.</li><li>Choose a station to explore forecast and source attribution.</li><li>Use reports to export a city snapshot.</li></ol></section><section><h2>Available local API</h2><ul>{endpoints.map(e=><li key={e}><code>{e}</code></li>)}</ul></section></div><p className="doc-note">When the API is running locally, interactive API documentation is available at <a href="http://localhost:8000/docs" target="_blank" rel="noreferrer">localhost:8000/docs <ExternalLink size={13}/></a>. <Link to="/contact">Send feedback</Link> if something is unclear.</p></main></PageShell>}
