import { useState } from "react";
import PageShell from "../components/PageShell";
import Breadcrumbs from "../components/Breadcrumbs";
export default function Contact(){const [sent,setSent]=useState(false);function submit(e){e.preventDefault();setSent(true);e.currentTarget.reset()}return <PageShell><main className="page-wrap narrow"><Breadcrumbs items={[{label:"Feedback"}]}/><span className="eyebrow">Help improve UrbanAQI</span><h1 className="page-title">Contact & feedback</h1><p className="page-lead">This local feedback form does not send data to an external service.</p>{sent?<div className="callout"><h2>Thanks for the feedback.</h2><p>Your message was captured for this browser session.</p><button className="button" onClick={()=>setSent(false)}>Send another</button></div>:<form className="feedback-form" onSubmit={submit}><label>Name<input required name="name" autoComplete="name" /></label><label>Email<input required name="email" type="email" autoComplete="email" /></label><label>Topic<select name="topic"><option>General feedback</option><option>Data quality</option><option>Accessibility</option><option>Feature request</option></select></label><label>Message<textarea required name="message" rows="5" /></label><button className="button button--primary">Submit feedback</button></form>}</main></PageShell>}
