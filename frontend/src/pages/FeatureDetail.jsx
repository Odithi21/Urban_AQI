import { Link, useParams } from "react-router-dom";
import { ArrowRight } from "lucide-react";
import PageShell from "../components/PageShell";
import Breadcrumbs from "../components/Breadcrumbs";
const content={"aqi-intelligence":["Real-time AQI intelligence","Monitor current conditions, station trends and forecasted changes before they turn into citywide incidents.",["Current ward AQI and station telemetry","24-hour historical trend view","Forecast confidence and pollutant context"]],"source-attribution":["Source attribution","Turn pollutant readings into an explainable breakdown of traffic, construction, industrial and weather contributions.",["Station-level source percentages","Plain-language rationale","Actionable mitigation recommendations"]],"health-risk":["Health risk mapping","Pair air quality and population exposure signals to communicate the right precaution at the right time.",["Ward-specific advisories","Exposure-aware priority ranking","Outdoor, mask and activity guidance"]]};
export default function FeatureDetail(){const {feature}=useParams();const [title,intro,items]=content[feature]||content["aqi-intelligence"];return <PageShell><main className="page-wrap"><Breadcrumbs items={[{label:"Features",to:"/"},{label:title}]}/><span className="eyebrow">Feature deep dive</span><h1 className="page-title">{title}</h1><p className="page-lead">{intro}</p><div className="detail-list">{items.map((item,i)=><div key={item}><b>0{i+1}</b><span>{item}</span></div>)}</div><Link to="/dashboard" className="button button--primary">Explore live data <ArrowRight size={16}/></Link></main></PageShell>}
